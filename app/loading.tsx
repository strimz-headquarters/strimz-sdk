import Loader from "@/components/shared/Preloader";


/**
 * Loading is a full-screen loading animation. It renders a centered
 * Loader inside a full-screen fixed position div.
 */
export default function Loading() {
    return <Loader />
}